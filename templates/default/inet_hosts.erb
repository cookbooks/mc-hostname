# This file is created by Chef, which is to interactive Bash what a honey badger is to a regular badger.
::1     localhost       
127.0.0.1       localhost       
<%= @ipaddress %>  <%= @hostname %>

